/** @format */

import React from "react"
import { SafeAreaView, View, Text } from "react-native"

const FavoritesScreen = (params) => {
  return (
    <SafeAreaView>
      <Text>NIY Favorites</Text>
    </SafeAreaView>
  )
}

export default FavoritesScreen
